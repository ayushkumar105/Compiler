#include "infToPstf.h"
struct tableEntry { 
    int symbol;
    char type;      /* 'C', 'L' or 'V' */
    int location;   /* 00 to FF */ 
};

typedef struct tableEntry TableEntry;

struct pCompiler {
    int memsize;
	TableEntry *symTab;         /* the symbol table */
	unsigned short
